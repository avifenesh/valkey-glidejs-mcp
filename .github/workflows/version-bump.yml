# DISABLED: Manual tag-based releases only
# This workflow has been disabled to prevent automatic version bumping
# Releases are now triggered only on manual tag pushes

name: Version Bump (Disabled)

# on:
#   push:
#     branches:
#       - main
#   workflow_dispatch:

# Disabled - use manual tagging instead

# permissions:
#   contents: write
#   pull-requests: write

# jobs:
#   bump-version:
#     if: "!contains(github.event.head_commit.message, '[skip ci]')"
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0
#           token: ${{ secrets.GITHUB_TOKEN }}

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "20"
#           registry-url: "https://registry.npmjs.org"

#       - name: Conventional Changelog Action
#         id: changelog
#         uses: TriPSs/conventional-changelog-action@v5
#         with:
#           github-token: ${{ secrets.GITHUB_TOKEN }}
#           git-message: "chore(release): {version} [skip ci]"
#           git-user-name: "github-actions[bot]"
#           git-user-email: "github-actions[bot]@users.noreply.github.com"
#           preset: "conventionalcommits"
#           tag-prefix: "v"
#           output-file: "CHANGELOG.md"
#           release-count: 0
#           version-file: "./package.json"
#           create-summary: true
#           skip-on-empty: true
#           skip-version-file: false
#           skip-commit: false

#       - name: Create Release
#         if: ${{ steps.changelog.outputs.skipped == 'false' }}
#         uses: ncipollo/release-action@v1
#         with:
#           tag: ${{ steps.changelog.outputs.tag }}
#           name: ${{ steps.changelog.outputs.tag }}
#           body: ${{ steps.changelog.outputs.clean_changelog }}
#           token: ${{ secrets.GITHUB_TOKEN }}
